{
  "name": "one-next-task",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "npm run build && node --test storage.test.mjs tests/worker.test.mjs"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
